# =========================
# Supabase (required for core features)
# =========================

# Supabase project URL (safe to be public)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anon key (safe to be public; access is limited by Supabase RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only key (DANGEROUS). Never expose to the browser.
# Must NOT start with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Optional: direct database URL (only if we later use server-side DB connections)
# DATABASE_URL=
